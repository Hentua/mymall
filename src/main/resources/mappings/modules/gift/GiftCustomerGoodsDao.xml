<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.modules.gift.dao.GiftCustomerGoodsDao">
    
	<sql id="giftCustomerGoodsColumns">
		a.id AS "id",
		a.gift_customer_id AS "giftCustomerId",
		a.goods_id AS "goodsId",
		a.goods_count AS "goodsCount",
		a.merchant_code AS "merchantCode",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="giftCustomerGoodsJoins">
		LEFT JOIN gift_customer b ON b.id = a.gift_customer_id
	</sql>
    
	<select id="get" resultType="GiftCustomerGoods">
		SELECT 
			<include refid="giftCustomerGoodsColumns"/>
		FROM gift_customer_goods a
		<include refid="giftCustomerGoodsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="GiftCustomerGoods">
		SELECT 
			<include refid="giftCustomerGoodsColumns"/>
		FROM gift_customer_goods a
		<include refid="giftCustomerGoodsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="giftCustomerId != null and giftCustomerId != ''">
				AND a.gift_customer_id = #{giftCustomerId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="GiftCustomerGoods">
		SELECT 
			<include refid="giftCustomerGoodsColumns"/>
		FROM gift_customer_goods a
		<include refid="giftCustomerGoodsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO gift_customer_goods(
			id,
			gift_customer_id,
			goods_id,
			goods_count,
			merchant_code,
			del_flag
		) VALUES (
			#{id},
			#{giftCustomerId},
			#{goodsId},
			#{goodsCount},
			#{merchantCode},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE gift_customer_goods SET 	
			gift_customer_id = #{giftCustomerId},
			goods_id = #{goodsId},
			goods_count = #{goodsCount},
			merchant_code = #{merchantCode}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE gift_customer_goods SET 
			del_flag = #{DEL_FLAG_DELETE}
		<choose>
			<when test="id !=null and id != ''">
				WHERE id = #{id}
			</when>
			<otherwise>
				WHERE gift_customer_id = #{giftCustomerId}
			</otherwise>
		</choose>
	</update>
	
</mapper>