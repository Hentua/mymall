<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.modules.member.dao.MerchantCollectionInfoDao">
    
	<sql id="merchantCollectionInfoColumns">
		a.id AS "id",
		a.user_id AS "userId",
		a.merchant_id AS "merchantId",
		a.create_date AS "createDate"
	</sql>
	
	<sql id="merchantCollectionInfoJoins">
	</sql>
    
	<select id="get" resultType="MerchantCollectionInfo">
		SELECT 
			<include refid="merchantCollectionInfoColumns"/>
		FROM merchant_collection_info a
		<include refid="merchantCollectionInfoJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="MerchantCollectionInfo">
		SELECT 
			<include refid="merchantCollectionInfoColumns"/>
		FROM merchant_collection_info a
		<include refid="merchantCollectionInfoJoins"/>
		<where>
			
			<if test="userId != null and userId != ''">
				AND a.user_id = #{userId}
			</if>
			<if test="merchantId != null and merchantId != ''">
				AND a.merchant_id = #{merchantId}
			</if>
			<if test="createDate != null and createDate != ''">
				AND a.create_date = #{createDate}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="MerchantCollectionInfo">
		SELECT 
			<include refid="merchantCollectionInfoColumns"/>
		FROM merchant_collection_info a
		<include refid="merchantCollectionInfoJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO merchant_collection_info(
			id,
			user_id,
			merchant_id,
			create_date
		) VALUES (
			#{id},
			#{userId},
			#{merchantId},
			#{createDate}
		)
	</insert>
	
	<update id="update">
		UPDATE merchant_collection_info SET 	
			user_id = #{userId},
			merchant_id = #{merchantId}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM merchant_collection_info
		WHERE id = #{id}
	</update>
	
</mapper>